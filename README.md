# Cairn

A small CLI + one config file that turns Claude Code's scattered primitives (skills, per-scope settings, per-subagent model, `~/.claude`) into a portable, toggleable, cross-machine vault — with cheap local-model delegation and warm-start memory.

See [SPEC.md](SPEC.md) for the v0 design.
